{
  "name": "mm-coa-wxa",
  "version": "0.0.1",
  "keywords": [
    "coajs",
    "coa",
    "wxa"
  ],
  "license": "MIT",
  "scripts": {
    "dev": "tsc -w",
    "build": "rm -rf dist* && tsc && cp package.json *.md dist",
    "sync": "curl -X PUT 'https://registry.npmmirror.com/mm-coa-wxa/sync?publish=false&nodeps=false'",
    "lint": "echo lint",
    "prettier": "prettier -w ."
  },
  "dependencies": {
    "dayjs": "^1.10.6"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^3.12.0",
    "prettier": "^2.3.2",
    "typescript": "^4.3.5"
  }
}